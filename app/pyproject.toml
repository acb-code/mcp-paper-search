[project]
name = "paper-archive-mcp"
version = "0.1.0"
description = "MCP server demo: search a local paper archive"
requires-python = ">=3.12"
dependencies = [
  "mcp[cli]>=1.0.0",
  "pypdf>=5.0.0",
]

[project.scripts]
paper-archive-mcp = "server:main"